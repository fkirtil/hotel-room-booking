spring:
  datasource:
    url: jdbc:h2:mem:test
    username:
    password:
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false